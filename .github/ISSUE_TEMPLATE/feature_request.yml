name: Feature request
description: Suggest a new feature or enhancement
title: "[feature]: "
labels:
  - enhancement
  - needs-triage
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to suggest a feature. Before filing, please check the [issue tracker](https://github.com/decibri/decibri-cli/issues) for existing requests.

  - type: textarea
    id: problem
    attributes:
      label: What problem does this solve?
      description: Describe the use case. What are you trying to do that is currently hard or impossible?
      placeholder: |
        I'm building a CI pipeline that needs to [...] but decibri-cli currently [...]
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: Proposed solution
      description: If you have a specific design in mind, describe it. Otherwise leave blank.
      placeholder: |
        Add a `--foo` flag to `decibri capture` that [...]
    validations:
      required: false

  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered
      description: Other approaches you evaluated and why they did not fit.
    validations:
      required: false

  - type: textarea
    id: scope
    attributes:
      label: Version scope
      description: Is this a small addition, a new feature, or a breaking change to existing commands? Best guess is fine.
    validations:
      required: false

  - type: textarea
    id: additional
    attributes:
      label: Additional context
      description: Examples, related tools, prior art, anything else useful.
    validations:
      required: false
